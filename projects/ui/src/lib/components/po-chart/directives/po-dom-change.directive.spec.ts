import { PoDomChangeDirective } from './po-dom-change.directive';

describe('PoDomChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new PoDomChangeDirective();
    expect(directive).toBeTruthy();
  });
});
